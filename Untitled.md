请根据 `CLAUDE.md`、`PRD` 和 `plan`，生成适合项目开发跟进的`README.md` 和 `todo.md`。

## 一、读取要求

必须先完整阅读：

- `PRD`
- `plan`

再生成文档。

如果文件中没有提到某个功能，不要凭空新增。  
如果信息缺失，请在文档中标记为“待确认”。

---

## 二、整体目标

生成 2 个文档：


- `README.md`
- `todo.md`


---
## 三、todo.md拆分规则

任务必须以“功能模块”为一级结构进行拆分，例如：用户登录模块、商品管理模块、订单模块、后台管理模块等。
不要把整个 todo.md 直接按 Controller / Service / Mapper / Repository 作为一级目录拆分。
在每个功能模块内部，再按照以下结构细分任务：

1. 模块目标
2. 用户可见内容
3. 用户操作流程
4. 前端页面任务
5. Controller 任务
6. Service 任务
7. Mapper 任务
8. Repository / 数据保存任务
9. 页面测试方法
10. 接口测试方法
11. 异常情况测试
12. 当前状态


---

## 四、任务粒度要求

每个任务必须尽量详尽、原子化、有依赖关系，并且可以被 AI 直接执行。

要求：

- 一个任务只修改或创建一个主要文件
- 不要出现“完成整个用户模块”这种大任务
- 每个任务都要写清楚涉及文件
- 每个任务都要写清楚前置依赖
- 每个任务都要写清楚验收方式
- 每个任务都要适合 AI 一条一条执行

---

## 五、状态规则

任务状态只能使用以下几种：

- 未开始
- 进行中
- 已完成
- 阻塞
- 待确认

验收状态只能使用以下几种：

待验收
自动验收通过
人工验收通过
验收驳回
重新提交
无需验收


## 六、每个模块必须说明的问题

每个模块都必须写清楚：

- 用户从页面上能看到什么
- 用户能点击什么
- 点完之后预期发生什么
- 对应后端接口是什么
- 数据保存在哪里
- 如果失败，可能是哪一层出问题

如果当前模块属于第一阶段基础模块，没有具体页面或业务操作，也要说明：

- 当前阶段用户看不到具体业务功能
- 该模块主要用于支撑后续功能开发
- 可以通过启动项目、访问默认页面或调用健康检查接口验证

---

## 七、测试要求

不要强制复杂 TDD。

但是每个功能模块必须提供：

1. 页面测试方法
2. 接口测试方法
3. 异常情况测试

测试方式要让非技术用户也能理解。

例如：

| 步骤 | 操作         | 预期结果         |
| ---- | ------------ | ---------------- |
| 1    | 启动前端项目 | 页面可以正常打开 |
| 2    | 打开首页地址 | 能看到默认首页   |
| 3    | 点击测试按钮 | 页面有响应或提示 |

---




---

## 九、README.md 要求

`README.md` 需要包含：

1. 项目简介
2. 适合谁使用
3. 项目主要功能
4. 项目目录结构说明
5. 本地运行方式
6. 环境变量说明
7. 常用命令
8. 文档索引
9. 开发流程说明
10. 如何测试功能是否正常

要求：

- 不要写成纯技术文档
- 要让非技术用户知道项目怎么启动、怎么看、怎么测
- 重点说明：
  - 去哪里看页面
  - 点哪里测试功能
  - 出错时先看哪里

---

最终请生成并写入以下文件：


- `README.md`
- `todo.md`

